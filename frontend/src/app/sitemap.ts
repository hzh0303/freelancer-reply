import type { MetadataRoute } from 'next';import { site, routes } from '@/lib/site';export default function sitemap(): MetadataRoute.Sitemap {return [routes.home,routes.tool,routes.privacy,routes.terms,routes.cookies,routes.refund].map(route=>({url:`${site.url}${route}`,lastModified:new Date(),changeFrequency:route==='/'?'weekly':'monthly',priority:route==='/'?1:route===routes.tool?.valueOf()?0.95:0.4}))}