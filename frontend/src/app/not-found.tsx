import Link from 'next/link';import { PageShell } from '@/components/layout/PageShell';import { routes } from '@/lib/site';export default function NotFound(){return <PageShell><section className="section text-center"><h1 className="font-display text-5xl">Page not found.</h1><p className="mt-4 muted">The page you are looking for may have moved, or the tool may not be available yet.</p><div className="mt-8 flex justify-center gap-3"><Link className="btn btn-primary" href={routes.tool}>Generate a late payment reminder</Link><Link className="btn btn-secondary" href={routes.home}>Back to homepage</Link></div></section></PageShell>}