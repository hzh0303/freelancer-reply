import Link from 'next/link';
import { routes, site } from '@/lib/site';
export function Header(){return <header className="sticky top-0 z-40 border-b border-[var(--border)] bg-[var(--card)]/95 backdrop-blur"><div className="mx-auto flex h-20 max-w-[1200px] items-center justify-between gap-4 px-4 md:px-10"><Link href={routes.home} className="flex items-center gap-2 font-display text-2xl font-semibold text-[var(--ink)]"><span aria-hidden className="grid h-9 w-9 place-items-center rounded-lg bg-[var(--primary)] text-white">✎</span>{site.name}</Link><nav className="hidden lg:flex items-center gap-7 text-sm font-medium muted"><Link href={routes.tool}>Late Payment Reminder</Link><Link href="/#example">Examples</Link><Link href="/#pricing">Pricing</Link><Link href="/#faq">FAQ</Link></nav><div className="flex items-center gap-2"><Link href="/#waitlist" className="hidden lg:block px-3 py-2 text-sm font-semibold muted">Pro waitlist</Link><Link href={routes.tool} className="btn btn-primary hide-tiny !w-auto px-4 py-3">Generate reminder</Link></div></div></header>}
