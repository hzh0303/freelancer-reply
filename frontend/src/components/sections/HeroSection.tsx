import Link from 'next/link';import { routes } from '@/lib/site';import { example } from '@/data/content';
export function HeroSection(){return <section className="section grid items-center gap-14 lg:grid-cols-[1.05fr_.95fr] mobile-stack"><div><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-[var(--border)] bg-[var(--primary-soft)] px-4 py-2 text-xs font-bold uppercase tracking-wide muted"><span className="h-2 w-2 rounded-full bg-[var(--primary)]"/>Free beta • 3 generations/day</div><h1 className="font-display text-[56px] font-semibold leading-[1.05] tracking-[-.03em]">Freelance Email Generator for Awkward Client Conversations</h1><p className="mt-6 max-w-xl text-lg leading-8 muted">Start with a polite late payment reminder. Enter a few invoice details, choose a tone, and get email drafts for Gentle, Firm, and Final Notice follow-ups.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Link className="btn btn-primary" href={routes.tool}>Generate a late payment reminder</Link><Link className="btn btn-secondary" href="#example">See example email</Link></div><p className="mt-4 text-sm muted">✓ Nothing is sent automatically. You stay in control of what gets sent.</p></div><div className="paper-card p-5 md:p-7"><div className="mb-5 flex items-center justify-between border-b border-[var(--border)] pb-4"><h2 className="font-display text-2xl">Invoice Follow-up</h2><span className="rounded-full bg-[var(--primary-soft)] px-3 py-1 text-xs font-bold muted">Draft Mode</span></div><div className="grid grid-cols-2 gap-3 text-sm"><Readonly label="Client" value={example.clientName}/><Readonly label="Amount" value={example.amount}/><Readonly label="Days" value={`${example.daysOverdue}`}/><Readonly label="Project" value={example.projectType}/></div><div className="my-5 flex flex-wrap gap-2"><span className="chip">Friendly</span><span className="chip chip-active">Professional</span><span className="chip">Firm</span><span className="chip border-red-200 text-red-700">Final Notice</span></div><pre className="max-h-64 overflow-auto whitespace-pre-wrap rounded-xl border border-[var(--border)] bg-[var(--paper)] p-4 text-sm leading-6">{example.body}</pre></div></section>}
function Readonly({label,value}:{label:string,value:string}){return <label><span className="label">{label}</span><input className="input mt-1" readOnly value={value}/></label>}
